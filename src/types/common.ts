// Tipos comunes y utilidades

// Theme string literal (light/dark/custom) - Para uso interno
// NOTA: El tipo Theme completo está en types/theme.ts
export type Theme = 'light' | 'dark' | 'custom';

export type ColorKey = string; // Key de $df-colors

// Tamaños estándar para todos los componentes (alineado con tokens.ts)
export type Size = 'x-small' | 'small' | 'default' | 'large' | 'x-large';
